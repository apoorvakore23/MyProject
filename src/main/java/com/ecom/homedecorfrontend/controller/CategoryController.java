package com.ecom.homedecorfrontend.controller;

import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

import com.ecom.homedecorfrontend.models.Category;

public class CategoryController {
//@ModelAttribute=> when java domain object sendothrwise use
	/*Logger log=new  L
			Model
	@RequestMapping(value="/categories",method=RequestMethod.GET)
	public String listCategories(Model model){
		log.debug("start of model list categories");
		model.addAllAttributes("category",category);
		model.addAllAttributes("categoryList",categoryDAO.list());
		log.debug("end of model list categories");
		return "category";
	}
	@RequestMapping(value="/category/add",method=RequestMethod.GET)
	public String addCategory(@ModelAttribute(""category)Category category){
		if(categoryDAO.get(category.getId())==null){categoryDAO.save(category);}
		
		else{mv.addObject("errorMessage","The record exist with this id"+category.getId());
		}
		return "category";
	}
	*/
	
	












}
	
	
	
